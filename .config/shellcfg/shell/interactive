# put your aliases here

# Load aliases.
ALIAS=~/.config/shellcfg/alias.sh

if [ -f $ALIAS ]; then
	source $ALIAS
fi

# if in a terminal
#if [ -t 0 ]; then
    # other users shouldn't be able to message by default
    #mesg n

    # enable colours
#    if [ `tput colors` -ge 8 ]; then
#    fi
#fi
# Set a basic bash prompt
export PS1="\u@\h:\w\\$ "

